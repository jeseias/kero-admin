aside.aside
  nav.nav
    ul.nav__list
      - 
        const links = [
          { name: 'Encomendas', link: 'orders', icon: 'home' },
          { name: 'Produtos', link: 'products', icon: 'home' },
          { name: 'Clientes', link: 'clients', icon: 'home' },
          { name: 'Mensagens', link: 'messages', icon: 'home' }
        ]
      
      each link in links
        li.nav__item 
          a.nav__link(href=`#${link.link}`)
            span= link.name
            svg.nav__icon
              use(xlink:href=`src/assets/SVGs/sprite.svg#icon-${link.icon}`)    
            