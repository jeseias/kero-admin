import App from '../App'

import DOM from '../views/elements'

export const setUpHeader: () => void = () => {
  const { name, img, menuBtn } = DOM.header.user
  const Admin = App.AppData.loggedUser 

  
}